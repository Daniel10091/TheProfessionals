import { AppConfig } from "../src/models/AppConfig";

const appConfig: AppConfig = {
  name: 'TheProfessionals',
  version: '1.0.0',
  description: 'Your professional portfolio.',
  theme: 'dark',
  statusBarStyle: 'light',
}

export default appConfig;
